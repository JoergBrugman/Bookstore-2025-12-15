interface "BSB Book Type Process V2" extends "BSB Book Type Process"
{
    // In diesem Interface werden die bisherigen Procedures aus "BSB Book Type Process" 
    // und die folgende erwartet!
    procedure StartQualityCheck();
}